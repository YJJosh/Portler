# portler ports

Show the ports assigned to this project's services.

```bash
portler ports
```

Prints the `SERVICE / PORT / URL` table from the current assignments, including the [proxy](/guide/proxy) when configured. See [Ports](/guide/ports) for how assignment works.
